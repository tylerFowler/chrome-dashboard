import PanelTheme, { defaultTheme } from '../panel/panelTheme';

export default {
  ...defaultTheme,
  primaryColor: '#2d72d9',
  primaryColorRGB: { red: 45, green: 114, blue: 217 },
  fontColor: '#fff',
} as PanelTheme;
